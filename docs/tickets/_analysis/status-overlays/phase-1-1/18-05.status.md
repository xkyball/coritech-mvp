# Status Overlay - 18-05 Ticket 18.05 - Active Organization and Role Context

## Ticket File

docs/tickets/phase-1-1/18-05-active-organization-role-context.md

## Current Status

IMPLEMENTED

## Confidence

HIGH

## Ticket Snapshot

### Objective

Ensure every user action runs under an explicit active organization and role context so workflow proof and permissions remain unambiguous.

### Scope

Determine active organization on login | Create organization/role switcher for multi-context users | Persist active context in session or secure app state | Expose active context to API/service layer | Display active context in app shell | Use active context in audit/proof attribution | Handle users with no valid active organization

### Functional Requirements

Single-role users get context automatically | Multi-role users can switch context | User always sees which organization/role they are acting as | Permissions are evaluated against active context | Actions record active organization and role

### Technical Requirements

Do not trust client-selected context without server validation | Context switch must validate UserOrganizationRole membership | Centralize active context resolver | Avoid passing organizationId from arbitrary UI input when it can be derived from context | Create context endpoint if needed | Create switch active context action if needed | Expose current context to frontend | User cannot switch to organization/role they do not hold | All proof/audit events must use validated active context | Admin context must be clearly indicated

### Acceptance Criteria

Active context exists for authenticated user | Single-context user gets automatic context | Multi-context user can switch context | Invalid context switch is rejected | App shell displays active organization and role | API/services can access validated active context | Audit/proof hooks can use active context | Permission checks use active context

### Required Tests

Test context resolution for single-role user | Test context switch for multi-role user | Test rejection of unauthorized context | Test API permission check against active context

### Documentation Updates

Document active-context pattern in role/permission docs

### Out Of Scope

AI insights or predictive analytics | Blockchain, token, wallet or digital asset logic | Full Equine Data Space automation | Federation / studbook automation beyond placeholders | Sensor or wearable ingestion | Complex marketplace automation | Unrestricted buyer access | Real card payment processing or storage of sensitive payment data | Full external logistics-provider implementation unless explicitly scoped

### Dependencies

01-01-user-organization-role-model.md, 02-02-rbac-middleware.md

## Code Evidence

- apps/web/app/app/select-role/page.tsx renders active role selection.
- apps/web/app/app/context/switch/route.ts switches active context.
- apps/web/features/auth/active-context-runtime.mjs validates active organization/role context cookies.

## Test Evidence

- apps/web/features/auth/active-context-runtime.test.mjs covers active context validation and switching.
- apps/web/features/auth/role-routing.test.mjs covers role landing behavior.
- npm test passed.

## Documentation Evidence

- docs/security/managed-auth-provider.md and docs/architecture/web-app-shell-and-navigation.md document active role behavior.

## Missing Requirements

- No missing core active context acceptance evidence found in the current codebase.

## Blockers

- No hard blockers identified.

## Recommended Action

- Keep this ticket closed; reuse the active context guard for future routes.

## Suggested Next Codex Prompt

Implement docs/tickets/phase-1-1/18-05-active-organization-role-context.md only. Treat the ticket as the source of truth. Use docs/tickets/_analysis/status-overlays/phase-1-1/18-05.status.md for current implementation evidence. Do not weaken acceptance criteria, do not implement out-of-scope features, add/update focused tests and docs where relevant, run safe verification commands, and commit the changes with a meaningful message.
