# Status Overlay - 02-02 Ticket 2.2 - Implement RBAC permission middleware

## Ticket File

docs/tickets/phase-1/02-02-rbac-middleware.md

## Current Status

PARTIALLY_IMPLEMENTED

## Confidence

HIGH

## Ticket Snapshot

### Objective

Ensure role-specific access to the same proof infrastructure.

### Scope

Implement API permission middleware. | Enforce breeder, breeding station and platform admin access rules. | Apply role, organization and order-level checks. | Return 403 on unauthorized access. | Add tests for allowed and denied cases.

### Functional Requirements

Not extracted.

### Technical Requirements

Not extracted.

### Acceptance Criteria

Breeder can view/create own orders only. | Breeding Station can manage own listings and assigned orders. | Platform Admin access is controlled and logged. | Unauthorized access returns 403. | Permission failures are logged where relevant. | Tests cover positive and negative access cases.

### Required Tests

Not extracted.

### Documentation Updates

Not extracted.

### Out Of Scope

Blockchain / token logic | AI insights or AI claims | Full marketplace automation | Full equine data space automation | Federation/studbook automation unless explicitly required | Sensor / wearable data ingestion | Unrestricted buyer access | Custom authentication when managed auth is available | Public unrestricted document links | Vendor-owned production-critical account assumptions

### Dependencies

Not stated.

## Code Evidence

- packages/domain/src/rbac/permissions.mjs implements permission helpers including document/order checks.
- apps/web/features/auth/active-context-runtime.mjs validates active organization and role context.
- apps/web/app/app/admin/layout.tsx, station/layout.tsx and breeder/layout.tsx enforce role-specific route access.
- apps/web/features/auth/server-session.ts loads internal memberships for request-time role checks.

## Test Evidence

- packages/domain/src/rbac/permissions.test.mjs covers core permission checks.
- apps/web/features/auth/active-context-runtime.test.mjs and role-routing.test.mjs cover context/role behavior.
- npm test passed.

## Documentation Evidence

- docs/security/role-permission-matrix.md documents the role matrix.
- docs/architecture/api-contract-and-service-conventions.md requires handlers to derive auth context before commands.

## Missing Requirements

- No single app-wide API middleware/proxy layer enforces RBAC for every future API route.
- Some enforcement is still route/service local.

## Blockers

- Needs a shared request guard pattern before large admin/API expansion.

## Recommended Action

- Finish runtime RBAC wiring for API and route handlers before broad admin/support actions.

## Suggested Next Codex Prompt

Implement docs/tickets/phase-1/02-02-rbac-middleware.md only. Treat the ticket as the source of truth. Use docs/tickets/_analysis/status-overlays/phase-1/02-02.status.md for current implementation evidence. Do not weaken acceptance criteria, do not implement out-of-scope features, add/update focused tests and docs where relevant, run safe verification commands, and commit the changes with a meaningful message.
