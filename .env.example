# CoriTech MVP environment placeholder.
# Copy this file to a local .env file when a future ticket requires runtime
# configuration. Never commit real secrets or credentials.

CORITECH_ENVIRONMENT=local
CORITECH_APP_URL=
CORITECH_API_URL=

# Add service-specific variables only when an approved ticket selects or
# integrates that service.
# EXTERNAL_SERVICE_NAME_API_KEY=
